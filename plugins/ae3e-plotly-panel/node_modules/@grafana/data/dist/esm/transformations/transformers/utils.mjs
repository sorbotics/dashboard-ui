function findMaxFields(data) {
  let maxFields = 0;
  for (const frame of data) {
    if (frame.fields.length > maxFields) {
      maxFields = frame.fields.length;
    }
  }
  return maxFields;
}

export { findMaxFields };
//# sourceMappingURL=utils.mjs.map
