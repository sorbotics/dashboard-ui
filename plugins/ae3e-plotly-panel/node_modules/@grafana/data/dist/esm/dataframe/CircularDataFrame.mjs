import { CircularVector } from '../vector/CircularVector.mjs';
import { MutableDataFrame } from './MutableDataFrame.mjs';

class CircularDataFrame extends MutableDataFrame {
  constructor(options) {
    super(void 0, (buffer) => {
      return new CircularVector({
        ...options,
        buffer
      });
    });
  }
}

export { CircularDataFrame };
//# sourceMappingURL=CircularDataFrame.mjs.map
