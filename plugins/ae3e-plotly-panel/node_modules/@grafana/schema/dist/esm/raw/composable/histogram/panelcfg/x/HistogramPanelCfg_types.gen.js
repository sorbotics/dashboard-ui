import { G as GraphGradientMode } from '../../../../../common.gen-C7aXIUXg.js';

const pluginVersion = "11.6.17";
const defaultOptions = {
  bucketCount: 30,
  bucketOffset: 0
};
const defaultFieldConfig = {
  fillOpacity: 80,
  gradientMode: GraphGradientMode.None,
  lineWidth: 1
};

export { defaultFieldConfig, defaultOptions, pluginVersion };
